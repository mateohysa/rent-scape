import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com', 'images.unsplash.com', 'res.cloudinary.com', 'loremflickr.com'],
  },
};

export default nextConfig;
